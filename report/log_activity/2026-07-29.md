# Log Activity — 29 Juli 2026 (Rabu)

**Fokus hari ini:** Task 3 Step 5 — membuat objek DDIC di sistem SAP: sepuluh tabel, enam index, lock object, dan lima objek number range.

**Task tersentuh:** Task 3 Step 5 (sebagian)

---

## Yang dikerjakan

### 1. Verifikasi silang data element sebelum mulai

Sebelum menyentuh SE11, dicek dulu apakah 18 data element hasil kerja kemarin sudah cukup untuk seluruh tabel.

Hasilnya: 18 data element di `data_elements.txt` semuanya terpakai di 10 file definisi tabel, dan tidak ada tabel yang mereferensi `ZCP_DE_*` di luar daftar. Aman untuk lanjut.

### 2. Membuat sepuluh tabel transparan di SE11

Dikerjakan satu per satu, tidak diborong sekaligus, supaya kalau ada error langsung ketahuan tabel mana penyebabnya.

| Urutan | Tabel | Data Class | Size | Catatan |
|:---:|---|:---:|:---:|---|
| 1 | `ZCP_USER` | APPL1 | 1 | Tabel pertama, sekalian belajar alur SE11 |
| 2 | `ZCP_BUYER` | APPL1 | 1 | Tanpa kejutan |
| 3 | `ZCP_COLOR_CODE` | APPL1 | 1 | Index `MAT` sempat terlewat, dibuat menyusul |
| 4 | `ZCP_REQUEST` | APPL0 | 1 | Mulai masuk wilayah data transaksi |
| 5 | `ZCP_REQUEST_ITM` | APPL0 | 1 | Butuh reference field untuk `MENGE` |
| 6 | `ZCP_SO_IMPORT` | APPL0 | 1 | Butuh reference field untuk `MENGE` |
| 7 | `ZCP_DCP_HDR` | APPL0 | 1 | 23 kolom, terpanjang |
| 8 | `ZCP_DCP_ITEM` | APPL0 | 1 | Primary key 3 kolom, `PANEL_ID` bukan key |
| 9 | `ZCP_PHOTO` | APPL0 | **3** | Gagal aktivasi, lihat masalah (b) |
| 10 | `ZCP_AUDIT_LOG` | APPL0 | **3** | Lancar |

Semua tabel: Delivery Class `A`, Table View Maintenance *Display/Maintenance Allowed*, Buffering *not allowed*, package `$TMP`.

### 3. Membuat enam secondary index

| Tabel | Index | Sifat | Field |
|---|:---:|:---:|---|
| `ZCP_COLOR_CODE` | `MAT` | Unique | MANDT, MATNR |
| `ZCP_REQUEST` | `SO` | Non-unique | MANDT, SO_NUMBER |
| `ZCP_DCP_HDR` | `REQ` | Non-unique | MANDT, REQUEST_ID |
| `ZCP_DCP_ITEM` | `PID` | Unique | MANDT, PANEL_ID |
| `ZCP_PHOTO` | `REF` | Non-unique | MANDT, REF_TYPE, REF_ID, PANEL_NUMBER |
| `ZCP_AUDIT_LOG` | `REF` | Non-unique | MANDT, REF_TYPE, REF_ID |

### 4. Lock object `EZCP_SO`

Dibuat di SE11 dengan primary table `ZCP_SO_IMPORT` dan lock mode `E` (exclusive, cumulative).

Satu penyesuaian dari isian default: SE11 otomatis menjadikan **seluruh** field key sebagai lock parameter, yaitu `MANDT`, `SO_NUMBER`, dan `SO_ITEM`. Baris `SO_ITEM` dihapus sehingga kunci bekerja di level nomor SO.

Alasannya, approve satu request adalah satu keputusan atas satu SO, bukan kumpulan keputusan lepas per item. Di tengah proses, sistem membaca ulang isi SO, mengecek material mana yang sudah pernah jadi DCP, lalu menulis beberapa baris sekaligus ke `ZCP_SO_IMPORT`. Kalau kuncinya per item, dua Admin yang berjalan bersamaan di SO yang sama akan saling membaca kondisi setengah jadi milik satu sama lain.

Aktivasi menghasilkan `ENQUEUE_EZCP_SO` dan `DEQUEUE_EZCP_SO`.

### 5. Number range di SNRO &mdash; objek selesai, interval belum

**Kendala:** field *Number length domain* di SNRO tidak menyediakan input help &mdash; F4 menjawab `no input help is available`. Nama domain harus diketik langsung.

Field itu sebenarnya cuma minta domain apa pun yang panjangnya sama dengan panjang nomor yang diinginkan; SAP hanya mengambil panjangnya. Objek standar SAP pun begitu, misalnya objek `MATERIALNR` menunjuk domain `MATNR`.

Sempat disiapkan rencana cadangan membuat empat domain sendiri (`ZCP_DOM_NR4`, `NR5`, `NR9`, `NR12`), tapi pengecekan di SE11 dengan pola `NUM*` menemukan domain bawaan SAP yang cocok, jadi tidak jadi bikin sendiri:

| Object | Domain | Panjang | To-year flag |
|---|:---:|:---:|:---:|
| `ZCP_COLOR` | `NUM5` | 5 | tidak |
| `ZCP_REQ` | `NUM4` | 4 | **ya** |
| `ZCP_DCP` | `NUM4` | 4 | **ya** |
| `ZCP_PHOTO` | `NUM9` | 9 | tidak |
| `ZCP_AUDIT` | `NUM12` | 12 | tidak |

Kelima objek dibuat dengan Warning % `10,0` dan **buffering tidak aktif**.

Buffering sengaja dimatikan. Kalau aktif, tiap server aplikasi mengambil jatah nomor di muka, akibatnya nomor jadi bolong dan tidak urut. Color Code dan DCP ID tercetak di dokumen dan panel fisik, jadi keterurutan lebih berharga daripada sedikit tambahan kecepatan.

**Interval `01` belum dibuat untuk kelima objek.** Ini yang dikerjakan berikutnya. Objek tanpa interval belum bisa dipakai &mdash; `NUMBER_GET_NEXT` akan gagal dengan `interval_not_found`.

### 6. Memperbarui dokumentasi repo

Plan file diberi blok *Checkpoint Terakhir*, checkbox Task 1 dan Task 2 ditandai selesai, Task 3 Step 5 ditandai sebagian dengan sub-checklist. Empat catatan jebakan aktivasi ditambahkan ke file definisi tabel yang bersangkutan.

Dibuat juga folder `report/` berisi checkpoint per task dan log harian ini. Awalnya log harian berupa satu file `log_activity.md`, lalu dipecah jadi `report/log_activity/YYYY-MM-DD.md` supaya tiap hari punya laporan sendiri.

`src/01_ddic/snro.txt` diperbarui: nama domain `NUM4`/`NUM5`/`NUM9`/`NUM12` dicatat, ditambah keterangan buffering dan dua peringatan operasional &mdash; interval per tahun untuk `ZCP_REQ`/`ZCP_DCP`, dan interval yang tidak ikut transport.

---

## Masalah yang muncul dan cara mengatasinya

### a. Warning `Enhancement category for table missing` di setiap tabel

Muncul tiap kali aktivasi. **Bukan error** — tabel tetap Active.

Diselesaikan lewat menu `Extras -> Enhancement Category` lalu pilih `Can Be Enhanced (Character-Type or Numeric)`.

Sempat dibiarkan dulu di `ZCP_USER` sambil memastikan apakah berpengaruh ke sistem. Kesimpulannya tidak berpengaruh ke runtime, tapi tetap dibereskan supaya log aktivasi bersih dan warning asli tidak tenggelam di antara warning palsu.

### b. `ZCP_PHOTO` gagal aktivasi — dua sebab sekaligus

Pesan lengkapnya:

```
TABL ZCP_PHOTO was not activated
Enhancement category for table missing
Enhancement category for include or subtype missing
ZCP_PHOTO-PHOTO_DATA: Too long for activation of 'not null' flag (>255)
Table ZCP_PHOTO must be created in the database
Check on table ZCP_PHOTO resulted in errors
```

**Sebab pertama:** `PHOTO_DATA` dicentang *Initial Values*. Flag itu berarti `NOT NULL` di database, dan field `RAWSTRING` tidak bisa diberi jaminan itu.
→ Hilangkan centang *Initial Values* khusus di baris `PHOTO_DATA`; field lain tetap dicentang.

**Sebab kedua:** Enhancement Category belum diisi, dan untuk tabel ini harus `Can Be Enhanced (Deep)` — bukan `Character-Type or Numeric` seperti sembilan tabel lain. Penyebabnya `RAWSTRING` termasuk tipe deep, datanya tidak duduk langsung di baris tabel.

Setelah dua perbaikan itu, aktivasi berhasil.

### c. `no input help is available` di field Number length domain (SNRO)

F4 tidak berfungsi di field itu. Bukan salah setting sistem &mdash; SNRO memang tidak menyediakan input help untuk `DOMLEN`, nama domain harus diketik langsung.

Diselesaikan dengan mencari domain yang cocok lewat `SE11 -> Domain -> F4 -> NUM*`, ketemu `NUM4`, `NUM5`, `NUM9`, `NUM12`.

### d. Index `MAT` di `ZCP_COLOR_CODE` terlewat

Ketahuan waktu mau membuat index `PID` di `ZCP_DCP_ITEM` — disebut "ini yang kedua kalinya pakai UNIQUE", padahal yang pertama belum pernah dibuat. Penyebabnya waktu tabel ketiga langkah index diberikan ringkas, penjelasan detailnya baru muncul di tabel keempat.

Dicek lewat `SE11 -> ZCP_COLOR_CODE -> Indexes...`, ternyata memang kosong. Langsung dibuat dengan sifat Unique dan field `MANDT`, `MATNR`.

**Pelajarannya:** setiap selesai satu tabel yang punya index, cek langsung lewat tombol *Indexes...*, jangan diasumsikan sudah jadi.

---

## Catatan teknis yang perlu diingat

1. **`MANDT` harus diketik manual di index.** SE11 tidak menambahkannya otomatis seperti pada primary key, dan harus jadi field pertama.
2. **Index UNIQUE menganggap dua nilai kosong sebagai duplikat.** Berlaku di `MAT` (`MATNR`) dan `PID` (`PANEL_ID`). Kalau nanti muncul dump `DUPLICATE KEY`, tersangka pertama adalah kolom yang lupa diisi, bukan data kembar beneran.
3. **Field `QUAN` wajib punya reference field.** `MENGE` (`MENGE_D`) di `ZCP_REQUEST_ITM` dan `ZCP_SO_IMPORT` harus menunjuk ke `MEINS` di tabel yang sama, lewat tab *Currency/Quantity Fields*.
4. **`QTY_TOTAL` di `ZCP_DCP_HDR` bertipe `INT4`, bukan `MENGE_D`,** karena isinya cacah panel fisik — bilangan bulat tanpa satuan. Jadi tabel itu tidak butuh tab Currency/Quantity Fields.
5. **Objek number range belum berguna tanpa interval.** Membuat objek di SNRO dan membuat intervalnya adalah dua tahap di layar berbeda. Berhenti di tahap pertama membuat `NUMBER_GET_NEXT` gagal dengan `interval_not_found`.
6. **Interval number range tidak ikut transport.** Objeknya ikut, intervalnya tidak, karena interval adalah data client. Harus dibuat ulang manual di QAS dan PRD.

---

## Commit

| Commit | Waktu | Isi |
|---|---|---|
| `73132d7` | 15:26 | `docs(ddic): checkpoint Task 3 - 10 tabel dan 6 index aktif di SE11` |
| `fd4e6d5` | 15:56 | `docs(report): tambah report checkpoint dan log activity harian` |

---

## Status akhir hari

Task 3 Step 5 selesai sebagian.

- [x] Sepuluh tabel transparan
- [x] Enam secondary index
- [x] Lock object `EZCP_SO`
- [~] Lima objek number range di SNRO — objeknya sudah, **intervalnya belum**
- [ ] Message class `ZCP` dengan 20 pesan di SE91
- [ ] Table Maintenance Generator untuk `ZCP_BUYER`

**Rencana berikutnya:** mengisi interval `01` untuk kelima objek number range. Jalurnya `SNRO -> Object -> Number ranges -> Change Intervals -> Insert Interval -> Save`. Kolom `Ext` jangan dicentang, dan `ZCP_REQ` serta `ZCP_DCP` punya kolom `Year` tambahan yang diisi `2026`.
